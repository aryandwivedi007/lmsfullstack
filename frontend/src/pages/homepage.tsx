import { Box, Typography } from "@mui/material";
import HeroComponent from "../components/HeroComponent";

const Home = () => {
  return (
    <Box>
      <HeroComponent/>
    </Box>
  );
};

export default Home;